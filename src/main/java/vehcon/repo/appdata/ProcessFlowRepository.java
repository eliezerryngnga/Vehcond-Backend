package vehcon.repo.appdata;

import org.springframework.data.jpa.repository.JpaRepository;

import vehcon.models.masters.ProcessFlow;

public interface ProcessFlowRepository extends JpaRepository<ProcessFlow, Integer>{

}
